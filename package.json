{
  "name": "full-data-table",
  "version": "1.0.0",
  "description": "This is an express app that serves up a react front-end. It also connects to a MySQL database via sequelize.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "dev": "nodemon js"
  },
  "author": "Sharkbyte",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "express-graphql": "^0.12.0",
    "graphql": "^16.5.0",
    "mysql2": "^2.3.3",
    "nodemon": "^2.0.16",
    "sequelize": "^6.20.1"
  },
  "engines": {
    "node": "16.x"
  }
}
